{# Loop through pagination #}
{% if view.collection and view.template %}
    {% include view.template ~ '.html.twig' with {'view': view} %}
{% endif %}

{% if header.view.items.params.pagination %}
    {% include 'partials/pagination.html.twig' with {'base_url': header.view.base_url, 'pagination': header.view.items.params.pagination} %}
{% endif %}